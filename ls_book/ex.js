//We get an error on line 6 because a constant value can not be changed.//

/*It prints 'bar' because we have declared a new variable inside the block that is only in
 scope within that block.*/

/*No because constants have the same scope as using the let keyword and therefore you can
 have a different constant inside a block scope.*/